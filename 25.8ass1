#include <stdio.h>
int main()
{
   char str[20];
   int count=1,i;
   printf("Enter the string:");
   gets(str);
   for(i=0;str[i]!='\0';i++)
   {
       if(str[i]==' ')
        count++;
   }
   printf("no.of words=%d",count);
   return 0;
}
